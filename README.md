# 🏦 FinExplain — Evidence-First AI for Financial & Loan Decisions

**FinExplain** is an enterprise-grade, Agentic RAG (Retrieval-Augmented Generation) and proactive loan policy analysis platform. It bridges the gap between unstructured loan contracts and reliable financial decisions by enforcing **deterministic calculations**, **claim-level verbatim evidence citations**, **conflict detection**, and **multi-dimensional confidence scoring**.

---

## 🌟 Why FinExplain?

Standard LLM pipelines fail on financial agreements due to arithmetic hallucinations, missed conditional clauses (e.g., prepayments, fee waivers), and ungrounded assumptions. FinExplain solves this through:

- **Evidence-First Verification**: Every fact extracted is classified as `EXPLICIT`, `CONDITIONAL`, `INFERRED`, `NOT_SPECIFIED`, or `CONFLICTING` and mapped to verbatim document excerpts with exact page citations.
- **Deterministic Math Engine**: Financial math (EMIs, amortization schedules, prepayment penalties, processing fees, total scenario costs) is computed by dedicated Python financial calculators, never generated by LLM arithmetic.
- **Proactive Risk & Missing Information Audits**: Automatically analyzes documents to detect hidden cost drivers, missing rate disclosures, ambiguous clauses, and conflicting terms before you sign.
- **"Before You Confirm" Checklist**: Generates prioritized pre-commitment checklists and tailored questions to ask the lender.
- **Built-in Inspection Console**: Includes a responsive, dark-mode web console for document upload, live querying, proactive review, and metric visualization.

---

## 🏗️ System Architecture

```
                                 ┌─────────────────────────────────────────┐
                                 │     FinExplain Web Console (UI)         │
                                 │      (Served at / and /app)             │
                                 └────────────────────┬────────────────────┘
                                                      │ HTTP / REST
                                 ┌────────────────────▼────────────────────┐
                                 │          FastAPI Backend Engine         │
                                 └────────────────────┬────────────────────┘
                                                      │
         ┌──────────────────────────────┬─────────────┴────────────────┬──────────────────────────────┐
         │                              │                              │                              │
┌────────▼─────────────┐     ┌──────────▼───────────┐     ┌────────────▼───────────┐     ┌────────────▼───────────┐
│ Supabase PostgreSQL  │     │   Pinecone Vector    │     │   Groq LLM Engine      │     │Deterministic Calculator│
│ • Metadata & Chunks  │     │ • Dense Index (384d) │     │ • Evidence-First       │     │ • EMI & Amortization   │
│ • BM25 Full-Text FTS │     │ • HuggingFace/Torch  │     │   Prompting (Llama-3)  │     │ • Scenario Simulation  │
└──────────────────────┘     └──────────────────────┘     └────────────────────────┘     └────────────────────────┘
         │                              │                              │                              │
         └──────────────────────────────┼──────────────────────────────┴──────────────────────────────┘
                                        │
                                 ┌──────▼──────────────────────────────────┐
                                 │  Grounding & Claim Verification Engine  │
                                 │  • Fact Extractor & Condition Detector  │
                                 │  • Conflict & Missing Info Detector     │
                                 │  • Multi-Dimensional Confidence Scorer  │
                                 │  • Risk Engine (LOW/MED/HIGH/CRITICAL)  │
                                 └─────────────────────────────────────────┘
```

---

## 🛠️ Technology Stack

| Layer | Technologies |
|---|---|
| **Backend & API** | Python 3.11+, [FastAPI](https://fastapi.tiangolo.com/), Pydantic v2, Uvicorn |
| **Embeddings** | SentenceTransformers (`all-MiniLM-L6-v2`, 384 dimensions), Hugging Face Inference API / PyTorch Fallback |
| **Hybrid Search** | [Pinecone](https://www.pinecone.io/) (Dense) + [Supabase PostgreSQL](https://supabase.com/) (`tsvector` BM25 Sparse) |
| **Reranking** | Cross-Encoder (`cross-encoder/ms-marco-MiniLM-L-6-v2`) |
| **LLM Engine** | [Groq API](https://groq.com/) (`llama3-70b-8192`, `llama-3.3-70b-versatile`) |
| **Document Processing** | PyMuPDF (`fitz`), Hierarchical Parent-Child Chunker |
| **Frontend** | Modern Vanilla HTML5/CSS3/JavaScript Dark-Mode Console (Zero build step needed) |
| **Testing** | Pytest (22 deterministic scenario tests covering calculations, conflicts, and verification) |

---

## 📂 Project Structure

```
fine-explain/
├── Dockerfile                                      # Container build definition
├── docker-compose.yml                              # Multi-container orchestration
├── requirements.txt                                # Python dependencies
├── sample_loan_details.pdf                         # Sample loan document for testing
├── frontend/
│   ├── index.html                                  # FinExplain Inspection & Debug Console UI
│   └── .env.example
│
└── backend/
    ├── .env.example                                # Environment variable template
    ├── tests/                                      # Automated test suite
    │   ├── conftest.py                             # Deterministic test fixtures & mock facts
    │   └── test_evidence_pipeline.py               # 22-Scenario RAG evidence verification tests
    │
    └── app/
        ├── main.py                                 # FastAPI Server & static UI route mounting
        │
        ├── api/                                    # REST Endpoints (FastAPI)
        │   ├── dependencies.py                     # DB & Auth injection
        │   ├── schemas.py                          # Pydantic request / response schemas
        │   └── routes/v1/
        │       ├── analysis.py                     # Proactive Review & Before Confirmation APIs
        │       ├── documents.py                    # PDF upload & ingestion
        │       ├── queries.py                      # Evidence-first Q&A API
        │       ├── products.py                     # Product management
        │       ├── hilt.py                         # Human-in-the-Loop task review
        │       └── feedback.py                     # User feedback collection
        │
        ├── core/                                   # Core configuration & domains
        │   ├── config.py                           # Pydantic settings & env validation
        │   ├── loan_categories.py                  # Standard loan fact schemas & EvidenceStatus enums
        │   ├── constants.py                        # System constants
        │   └── security.py                         # Security & token handling
        │
        ├── db/                                     # Database & persistence layer
        │   ├── supabase_client.py                  # Supabase client singleton
        │   ├── schema.sql                          # Supabase PostgreSQL DDL & BM25 indices
        │   └── repositories/
        │       ├── chunk_repo.py                   # BM25 full-text search & chunk storage
        │       ├── document_repo.py                # Document metadata management
        │       ├── product_repo.py                 # Financial product metadata
        │       ├── hilt_repo.py                    # HITL task persistence
        │       └── verified_answer_repo.py         # Grounded answers & audit logging
        │
        ├── external/                               # Third-party integrations
        │   ├── pinecone_client.py                  # Pinecone vector operations
        │   ├── groq_client.py                      # Groq LLM client
        │   └── huggingface_client.py               # Hugging Face Inference API / fallback
        │
        ├── ingestion/                              # Document processing pipeline
        │   ├── parser.py                           # PyMuPDF PDF parser with page tracking
        │   ├── chunker.py                          # Hierarchical parent-child text splitter
        │   ├── embedder.py                         # Dense vector embedding generator
        │   └── pipeline.py                         # Orchestrated ingestion workflow
        │
        ├── rag/                                    # Agentic RAG & Evidence Pipeline
        │   ├── orchestrator.py                     # Main RAG pipeline entry point
        │   ├── extraction/                         # Structured fact extraction & audits
        │   │   ├── fact_extractor.py               # Rule & regex-based loan fact extraction
        │   │   ├── condition_detector.py           # Conditional clause & prerequisite detection
        │   │   ├── cost_driver_detector.py         # Fee & interest cost driver analysis
        │   │   ├── missing_detector.py             # Missing disclosure detection
        │   │   ├── risk_engine.py                  # Risk severity scoring (0-100 score & level)
        │   │   ├── lender_questions.py             # Actionable borrower question generator
        │   │   └── loan_analyzer.py                # Complete document review coordinator
        │   ├── retrieval/                          # Hybrid retrieval engine
        │   │   ├── dense_retriever.py              # Pinecone vector similarity search
        │   │   ├── sparse_retriever.py             # Supabase BM25 tsvector search
        │   │   ├── hybrid_retriever.py             # Reciprocal Rank Fusion (RRF)
        │   │   └── reranker.py                     # Cross-encoder reranking
        │   ├── verification/                       # Grounding, claims & confidence
        │   │   ├── claim_verifier.py               # LLM output claim-by-claim verification
        │   │   ├── conflict_detector.py            # Section & document conflict detector
        │   │   ├── confidence.py                   # Multi-dimensional confidence scorer
        │   │   ├── response_validator.py           # Final answer validator
        │   │   └── grounder.py                     # Citation coverage & source verification
        │   └── generation/                         # LLM prompt templates & generation
        │       ├── generator.py                    # Groq generation with evidence constraint
        │       └── prompt_templates.py             # Strict evidence-first prompt templates
        │
        └── tools/                                  # Deterministic financial tools
            ├── calculator.py                       # EMI, loan scenario, and fee calculator
            ├── comparator.py                       # Multi-product comparison engine
            ├── retrieval_tool.py                   # Retrieval tool abstraction
            └── verification_tool.py                # Verification tool abstraction
```

---

## 🚀 API Endpoints Overview

| Method | Endpoint | Description |
|---|---|---|
| `POST` | `/api/v1/queries` | Ask a question against loan documents with hybrid retrieval and evidence verification. |
| `POST` | `/api/v1/analysis/review` | Proactive document review: extracts facts, conditions, cost drivers, and missing disclosures. |
| `POST` | `/api/v1/analysis/before-confirmation` | Generates a 10-point "Before You Confirm" borrower checklist with citations. |
| `POST` | `/api/v1/documents/upload` | Upload a PDF document; parses, chunks, embeds, and indexes into Pinecone and Supabase. |
| `GET` | `/api/v1/products` | Retrieve financial products and loan agreements. |
| `POST` | `/api/v1/products` | Create a new financial product entry. |
| `GET` | `/api/v1/hilt/tasks` | List pending Human-in-the-Loop review tasks. |
| `POST` | `/api/v1/feedback` | Submit user feedback on answer accuracy and citations. |
| `GET` | `/health` | Health check endpoint. |
| `GET` | `/` or `/app` | Interactive Web UI Console. |

---

## ⚙️ Environment Variables

Create a `.env` file in `backend/.env` (refer to [`backend/.env.example`](file:///d:/Projects/fine-explain/backend/.env.example)):

```env
# Supabase Configuration (PostgreSQL + BM25 Full Text Search)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-supabase-anon-or-service-key

# Pinecone Vector Database (Dense Vector Search)
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_INDEX_NAME=fine-explain

# Groq API (Evidence-First LLM Generation)
GROQ_API_KEY=your-groq-api-key

# Hugging Face (Optional: for cloud embeddings)
HUGGINGFACE_API_KEY=your-hf-api-key

# Redis (Optional: for task caching)
REDIS_URL=redis://localhost:6379/0
```

---

## 🗄️ Database Setup (Supabase)

1. Open your **Supabase Project Dashboard** and navigate to the **SQL Editor**.
2. Open [`backend/app/db/schema.sql`](file:///d:/Projects/fine-explain/backend/app/db/schema.sql).
3. Execute the SQL script to create tables (`users`, `products`, `documents`, `chunks`, `hilt_tasks`, `verified_answers`, `scenarios`) and the PostgreSQL `tsvector` BM25 index (`idx_chunks_tsv`).

---

## 💻 Quick Start & Running Locally

### 1. Set Up Virtual Environment & Install Dependencies
```bash
# Clone the repository
git clone https://github.com/samadhanmane/FinExplain.git
cd fine-explain

# Create virtual environment
python -m venv .venv

# Activate virtual environment
# Windows:
.venv\Scripts\activate
# Linux / macOS:
source .venv/bin/activate

# Install dependencies
pip install -r requirements.txt
```

### 2. Start the Backend & Web Console
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Access the Application
- 🖥️ **Web Inspection Console**: [http://localhost:8000/app](http://localhost:8000/app) (or [http://localhost:8000/](http://localhost:8000/))
- 📖 **Interactive Swagger API Docs**: [http://localhost:8000/docs](http://localhost:8000/docs)
- 📚 **ReDoc API Documentation**: [http://localhost:8000/redoc](http://localhost:8000/redoc)

---

## 🧪 Automated Testing Suite

FinExplain includes a comprehensive **22-scenario deterministic test suite** verifying calculation correctness, edge cases, citation auditing, and risk scoring without requiring external API credits:

```bash
# Run test suite
python -m pytest backend/tests/test_evidence_pipeline.py -v
```

### Test Coverage Highlights:
1. **Explicit Fee Extraction** — Accurately extracts stated percentage and fixed fees.
2. **Conditional Fees** — Detects fees triggered only under specific circumstances.
3. **Missing Disclosure Detection** — Identifies absent mandatory fee or APR terms.
4. **Conflicting Fee Clauses** — Detects contradictions across document sections.
5. **Early Repayment Penalty** — Analyzes lockout periods, formulas, and percentages.
6. **Fee Waiver Timing** — Correlates waivers against borrower tenure scenarios.
7. **Late Payment Penalties** — Evaluates penalty interest and grace period terms.
8. **Missing Late Payment Amount** — Flags missing penalty caps.
9. **Variable Interest Rate Rules** — Identifies benchmark spread mechanisms.
10. **Conflicting APR Terms** — Identifies APR discrepancies across agreements.
11. **Currency Conflict** — Identifies conflicting currencies across clauses.
12. **Prepayment Risk vs Scenario** — Flags early repayment penalties when scenario tenure is less than lockout period.
13. **Missing Calculation Input** — Prevents calculation with unverified parameters.
14. **Unsupported LLM Claim Penalty** — Penalizes confidence scores when LLM output lacks citation.
15. **Invalid Citation Page Verification** — Validates source page references against actual chunk pages.
16. **Deterministic Financial Math** — Verifies mathematical precision for EMI, interest, and total costs.
17. **Multi-Dimensional Confidence Scoring** — Evaluates evidence status weighting and penalization.
18. **Risk Severity Engine** — Validates composite risk scoring (0-100) and severity classifications.

---

## 🐳 Docker Deployment

To build and run using Docker:

```bash
# Build and start services
docker-compose up --build

# Run in detached mode
docker-compose up -d
```

The application and console will be available at `http://localhost:8000`.

---

## 📄 License

This project is licensed under the MIT License.